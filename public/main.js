function main() {
  menu.openFile.open({ name: 'label1' });
  return 0;
  // app.openWorkspace();

  // app.workspace.addComponent(new Input(100, 100));
  // app.workspace.addComponent(new Input(100, 300));
  // app.workspace.addComponent(new LogicGate("and", 300, 200));
  // app.workspace.addComponent(new Output(500, 200));

  // let tmp = new Label(100, 50);
  // tmp.text("Input A");
  // app.workspace.addComponent(tmp);

  // app.workspace.connectComponents(0, 0, 2, 0);
  // app.workspace.connectComponents(1, 0, 2, 1);
  // app.workspace.connectComponents(2, 0, 3, 0);

  return 0;
}