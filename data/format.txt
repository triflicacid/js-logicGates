{
    // Array of elements (components)
    "e": {
        "id", // Numerical ID of component
        "t", // Type of component (this.constructor.ID value)
        "l", // Component label
        "d", // Alternate data value for creation (e.g. logic gate type, label)
        "x", // X-coordinate
        "y" // Y-coordinate
    }[],

    // Array of connections
    // connection format: [src, src_index, dst, dst_index]
    "c"

    // "Advanced Option" data
    "o"
}